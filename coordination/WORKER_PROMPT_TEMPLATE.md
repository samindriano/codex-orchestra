# Optional bounded assignment

```text
Question / required deliverable:
Source folder and commit, where relevant:
Owned scope and prohibited changes:
Ready dependencies:
Verification and stopping condition:
```

Use runtime-pinned Luna XHigh roles and local children by default. Worktrees are
needed only for actual write collision. MAIN owns integration. Workers must not
spawn children, self-upgrade, merge, rebase, force-push, or create visible tasks.

Return a compact packet in the parent task; a separate handoff file is optional:

```text
VERDICT
Decision-changing findings
Exact file / run / commit references
Counterexample, if found
Remaining UNKNOWNs
Recommended next action
```
