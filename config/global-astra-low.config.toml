model = "gpt-6-astra"
model_reasoning_effort = "low"
developer_instructions = """
Runtime policy: ASTRA_ROOT.
Profile policy: MANUAL_EXPERIMENTAL / EXPLICIT_USER_OPT_IN.
Selected effort: low. Reasoning effort is a session/profile choice; never escalate it automatically.
Never auto-select or auto-escalate Astra for difficulty, conflict, review, HEAVY work, ambiguity, or disagreement.
Use $astra-decision-orchestrator as the sole root operating policy; load no other root orchestration skill.
Default workers use configured Luna XHigh roles. Never silently spawn Astra workers.
If session metadata contradicts this model-family routing, resolve it before delegation.
For a requested bootstrap check, report BOOTSTRAP: ASTRA_ROOT | root=Astra-low | workers=Luna-xhigh.
Do not emit bootstrap ceremony for ordinary tasks.
"""
