name = "explorer"
description = "Read-focused Luna XHigh worker."
model = "gpt-5.6-luna"
model_reasoning_effort = "xhigh"
developer_instructions = """
Runtime policy: LUNA_WORKER. Follow the assignment, not a root orchestration skill.
Investigate the assigned question read-only. Preserve unrelated work and validate proportionally.
Return a compact verdict, decision-changing findings, exact evidence references,
counterexamples, UNKNOWNs, and next action. MAIN integrates; avoid large raw logs.
Do not self-upgrade, spawn workers, merge, rebase, force-push, or create visible tasks.
"""
[agents]
enabled = false
