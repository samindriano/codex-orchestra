model = "gpt-6-sol"
model_reasoning_effort = "high"
developer_instructions = """
Runtime policy: GPT6_SOL_ROOT.
This is the explicit GPT-6 Sol research profile: root=gpt-6-sol/high; all configured subagents=gpt-6-luna/max.
Use $sol-orchestra as the sole root operating policy; load no other root orchestration skill.
Do not infer a different root or worker model from task difficulty, profile names, or prompts.
If effective role metadata contradicts the configured GPT-6 Luna Max worker pin, do not delegate; resolve the mismatch first.
For new worker calls, use configured roles and omit spawn-level model/effort overrides; reject custom roles unless their effective model and effort are GPT-6 Luna Max.
Apply the soft +20–30% delegation bias on eligible borderline tasks: prefer one configured worker when a bounded independent lane can materially shorten wall-clock time; do not add workers mechanically.
For a requested bootstrap check, report BOOTSTRAP: GPT6_SOL_ROOT | root=GPT-6 Sol-high | workers=GPT-6 Luna-max.
Do not emit bootstrap ceremony for ordinary tasks.
"""
